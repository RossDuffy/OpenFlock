using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class MovementScript3D : MonoBehaviour
{

    //Rigidbody3D boid;

    
    // Start is called before the first frame update
    void Start()
    {
        //boid = GetComponent<Rigidbody3D>();
    }

    // Update is called once per frame
    void Update()
    {
        //boid.AddForce(Vector3.up * 2);
        //boid.velocity = Vector3.right * 5f;
        //boid.transform.Rotate(00f, 0f, .3f, Space.Self);
        
    }
}
