using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class MovementScript : MonoBehaviour
{

    //Rigidbody2D boid;

    
    // Start is called before the first frame update
    void Start()
    {
        //boid = GetComponent<Rigidbody2D>();
    }

    // Update is called once per frame
    void Update()
    {
        //boid.AddForce(Vector2.up * 2);
        //boid.velocity = Vector2.right * 5f;
        //boid.transform.Rotate(00f, 0f, .3f, Space.Self);
        
    }
}
